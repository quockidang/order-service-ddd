apiVersion: v1
kind: Pod
metadata:
  name: kafka-client
  namespace: kafka
spec:
  containers:
    - name: kafka-client
      image: apache/kafka:4.1.1
      imagePullPolicy: IfNotPresent
      command:
        - sleep
        - infinity
